name: Build & Release MSI

on:
  workflow_dispatch
  
jobs:
  build-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write  
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@af1da67850ed9a4cedd57bfd976089dd991e2582 # v4.0.0

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build MSI with Gradle
        run: ./gradlew :client:packageMsi

      - name: Find MSI artifact
        run: |
          MSI_FILE=$(find client/build/compose/binaries -name "*.msi" | head -n 1)
          echo "MSI_FILE=$MSI_FILE" >> $GITHUB_ENV
          
      - name: Read client version
        run: echo "PROJECT_VERSION=$(./gradlew -q :client:printVersion)" >> $GITHUB_ENV
      - name: Upload MSI to Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ env.PROJECT_VERSION }}
          files: ${{ env.MSI_FILE }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
